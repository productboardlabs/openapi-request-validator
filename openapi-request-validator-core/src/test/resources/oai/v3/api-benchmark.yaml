openapi: 3.0.2
servers:
  - url: /
info:
  title: Benchmark
  version: 1.0.0
paths:
  '/benchmark':
    post:
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                field:
                  type: string
                  format: byte
      responses:
        '204':
          description: No content
