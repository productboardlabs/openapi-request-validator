openapi: 3.0.0
info:
  title: No components
  version: 1.0.0
paths:
  /theresource:
    get:
      summary: Get the resource
      responses:
        '200':
          description: Resource response
          content:
            application/json:
              schema:
                type: object
                properties:
                  id:
                    type: integer
                    format: int32
servers:
  - url: /
